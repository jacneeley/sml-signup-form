---
if (Astro.request.method == "POST"){
    try{
        const data = await Astro.request.formData();
        const parentName = data.get("parentName");
        const email = data.get("email");
        const parentPhone = data.get("parentPhone");
        const childName = data.get("childName");
        const childAge = data.get("childAge");
        
    } catch(err){
        if(err instanceof Error){
            console.error(err.message);
        }
    }
}
---

<div class='form-container'>
    <h2>Computer Science Camp </h2>
    <form method="POST">
        <label>Parent/Guardian's Full Name:</label>
        <input class="text-input" name="parentName" type="text" required/>

        <label>Guardian Email:</label>
        <input class="text-input" name="email" type="email" placeholder="email@email.com" required/>

        <label>Guardian Phone:</label>
        <input class="text-input" name="parentPhone" type="text" placeholder="For emergency contact only" maxlength="12" required/>
        
        <label>Child's Full Name:</label>
        <input class="text-input" name="childName" type="text" required/>
        
        <label>Child's Age:</label>
        <input class="text-input" name="childAge" type="text" min="10" required/>
        

        <p>A laptop is needed for this camp.The library can provide one to those who need one, but there is a limited quantity.</p>
        <p>Will your child be able to bring their own laptop?</p>
        
        <label class ="radio-label"><input type="radio" value="Yes" name="Yes" required/>Yes</label>
        <label class ="radio-label"><input type="radio" value="No" name="No" checked required/>No</label>
        
        <button class= "submit-btn" type="submit">Submit</button>
    </form>      
</div>